# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  repos:
  - $env/repos/spack_repo/hubcast
  specs:
  - py-pip
  - py-hubcast
  - smee-client
  - typos
  view: true
  concretizer:
    unify: true
  develop:
    py-hubcast:
      spec: py-hubcast@=main
      path: $env/../
